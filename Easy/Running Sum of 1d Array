class Solution:
    def runningSum(self, nums):
            self.result = []
            i = 1
            while i <= len(nums):
                self.result.append(sum(nums[0:i]))
                i +=1
            return self.result
